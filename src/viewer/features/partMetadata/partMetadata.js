/**
 * Part metadata feature barrel file.
 * Exports the hook for loading and caching part details from the metadata service.
 */
export { useSelectedPartMetadata } from "./hooks/useSelectedPartMetadata";
